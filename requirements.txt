# This Pip requirements file was automatically generated by the
# "check_READMEs.py" script from "https://github.com/Guymer/misc".

pillow
cartopy
exifread
geojson
lxml
matplotlib
meson # Required so that NumPy can use an up-to-date version (not the old
      # version that came with your system) when running "f2py".
ninja # Required so that NumPy can use an up-to-date version (not the old
      # version that came with your system) when running "f2py".
numpy
requests
scipy
shapely
sphinx
sphinx_rtd_theme
